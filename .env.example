OPENCLAW_IMAGE=openclaw:local
OPENCLAW_UID=1000
OPENCLAW_GID=1000
OPENCLAW_CONFIG_DIR=/home/USERNAME/agents/openclaw-data/config
OPENCLAW_WORKSPACE_DIR=/home/USERNAME/agents/openclaw-data/workspace
OPENCLAW_GATEWAY_PORT=18789
OPENCLAW_BRIDGE_PORT=18790
# Localhost-only exposure is enforced by docker-compose port bindings (127.0.0.1:...).
# This value affects OpenClaw's internal bind mode inside the container.
# Valid values include: loopback, lan, tailnet, auto, custom
OPENCLAW_GATEWAY_BIND=lan
OPENCLAW_GATEWAY_TOKEN=REPLACE_ME
# Optional: only if you intentionally use Claude web-session auth
# CLAUDE_AI_SESSION_KEY=
# CLAUDE_WEB_SESSION_KEY=
# CLAUDE_WEB_COOKIE=